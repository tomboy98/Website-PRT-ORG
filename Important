sudo apt install docker.io -y #Docker installation
sudo apt update
sudo apt install openjdk-11-jdk -y  #JAVA Installation
ansible -m ping -all
Install Docker Pipeline Plugin - In jenkins
Correct credentials should be configured
#Docker permissions added in KMaster
sudo groupadd docker
sudo usermod -aG docker $USER
newgrp docker
docker run hello-world
reboot
#Restart jenkins whenever any plugin or permissions is added
ASH-LATEST #pem file
TERRAFORM USER 
export AWS_ACCESS_KEY_ID=
export AWS_SECRET_ACCESS_KEY=
